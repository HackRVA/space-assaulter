import { Base } from 'canvas-screens';

export const Simulation = Object.create(Base);

Simulation.init = function() {
  this.units = [];
};

Simulation.addUnit = function(unit) {
};

Simulation.setTime = function(t) {
};

Simulation.getData = function(t) {
};

